/**
 * REST controllers exposing cluster observability and control endpoints on
 * port 8000: {@code ClusterHealthController} (/cluster/health),
 * {@code ClusterMetadataController} (/cluster/metadata),
 * {@code ConsumerStatsController} (/consumers/stats),
 * {@code FailureSimulationController} (/cluster/simulate-failure), and the
 * cluster-scoped {@code EventController} (/events/user/register).
 */
package com.streamsocial.eventtaxonomy.cluster.controller;
